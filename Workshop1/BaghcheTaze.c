#include <stdio.h>

int main(){
	float a,b,perimeter,area;
	scanf("%f %f", &a, &b);
	area = a*b;
	perimeter = 2*(a+b);
	printf("%f %f",perimeter,area);
	return 0;
}
#include <stdio.h>

int main(){
	float a,b,perimeter,area;
	scanf("%f %f", &a, &b);
	area = a*b;
	perimeter = 2*(a+b);
	printf("%f %f",perimeter,area);
	return 0;
}

